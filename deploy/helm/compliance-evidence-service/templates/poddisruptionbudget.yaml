apiVersion: policy/v1
kind: PodDisruptionBudget
metadata: { name: compliance-evidence-service, namespace: aegis-system }
spec:
  minAvailable: 50%
  selector: { matchLabels: { app.kubernetes.io/name: compliance-evidence-service } }
