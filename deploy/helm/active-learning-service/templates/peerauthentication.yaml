apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata: { name: active-learning-service-mtls-strict, namespace: aegis-system }
spec:
  selector: { matchLabels: { app.kubernetes.io/name: active-learning-service } }
  mtls: { mode: STRICT }
