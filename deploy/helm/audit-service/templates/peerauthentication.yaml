apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata: { name: audit-service-mtls-strict, namespace: aegis-system }
spec:
  selector: { matchLabels: { app.kubernetes.io/name: audit-service } }
  mtls: { mode: STRICT }
