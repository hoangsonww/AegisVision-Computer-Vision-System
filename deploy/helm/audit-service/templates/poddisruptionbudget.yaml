apiVersion: policy/v1
kind: PodDisruptionBudget
metadata: { name: audit-service, namespace: aegis-system }
spec:
  minAvailable: 50%
  selector: { matchLabels: { app.kubernetes.io/name: audit-service } }
