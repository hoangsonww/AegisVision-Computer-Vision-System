apiVersion: policy/v1
kind: PodDisruptionBudget
metadata: { name: event-service, namespace: aegis-system }
spec:
  minAvailable: 50%
  selector: { matchLabels: { app.kubernetes.io/name: event-service } }
