apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata: { name: event-service-mtls-strict, namespace: aegis-system }
spec:
  selector: { matchLabels: { app.kubernetes.io/name: event-service } }
  mtls: { mode: STRICT }
