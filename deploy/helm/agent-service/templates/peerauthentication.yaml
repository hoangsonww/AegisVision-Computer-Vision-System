apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata: { name: agent-service-mtls-strict, namespace: aegis-system }
spec:
  selector: { matchLabels: { app.kubernetes.io/name: agent-service } }
  mtls: { mode: STRICT }
