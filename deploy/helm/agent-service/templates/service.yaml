apiVersion: v1
kind: Service
metadata: { name: agent-service, namespace: aegis-system, labels: { app.kubernetes.io/name: agent-service } }
spec:
  selector: { app.kubernetes.io/name: agent-service }
  ports:
    - { name: http, port: 8420, targetPort: 8420, appProtocol: http }
    - { name: metrics, port: 8421, targetPort: 8421 }
