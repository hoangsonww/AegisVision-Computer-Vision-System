apiVersion: v1
kind: Service
metadata: { name: autonomy-orchestrator, namespace: aegis-system, labels: { app.kubernetes.io/name: autonomy-orchestrator } }
spec:
  selector: { app.kubernetes.io/name: autonomy-orchestrator }
  ports:
    - { name: http, port: 8510, targetPort: 8510, appProtocol: http }
    - { name: metrics, port: 8511, targetPort: 8511 }
