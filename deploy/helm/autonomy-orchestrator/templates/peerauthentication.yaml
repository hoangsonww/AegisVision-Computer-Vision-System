apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata: { name: autonomy-orchestrator-mtls-strict, namespace: aegis-system }
spec:
  selector: { matchLabels: { app.kubernetes.io/name: autonomy-orchestrator } }
  mtls: { mode: STRICT }
