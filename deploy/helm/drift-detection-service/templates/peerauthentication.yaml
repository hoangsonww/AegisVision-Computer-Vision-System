apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata: { name: drift-detection-service-mtls-strict, namespace: aegis-system }
spec:
  selector: { matchLabels: { app.kubernetes.io/name: drift-detection-service } }
  mtls: { mode: STRICT }
