apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata: { name: dataset-service-mtls-strict, namespace: aegis-system }
spec:
  selector: { matchLabels: { app.kubernetes.io/name: dataset-service } }
  mtls: { mode: STRICT }
