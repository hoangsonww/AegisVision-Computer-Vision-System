apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata: { name: training-orchestrator-mtls-strict, namespace: aegis-system }
spec:
  selector: { matchLabels: { app.kubernetes.io/name: training-orchestrator } }
  mtls: { mode: STRICT }
