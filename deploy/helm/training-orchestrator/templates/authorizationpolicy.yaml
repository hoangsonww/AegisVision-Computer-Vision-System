apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata: { name: training-orchestrator, namespace: aegis-system }
spec:
  selector: { matchLabels: { app.kubernetes.io/name: training-orchestrator } }
  action: ALLOW
  rules:
    - from:
        - source: { principals: [cluster.local/ns/aegis-system/sa/api-gateway] }
      to: [{ operation: { ports: ["8270"] } }]
    - from:
        - source: { principals: [cluster.local/ns/aegis-observability/sa/prometheus] }
      to: [{ operation: { ports: ["8271"] } }]
