apiVersion: policy/v1
kind: PodDisruptionBudget
metadata: { name: training-orchestrator, namespace: aegis-system }
spec:
  minAvailable: 50%
  selector: { matchLabels: { app.kubernetes.io/name: training-orchestrator } }
