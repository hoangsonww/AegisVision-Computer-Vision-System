apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata: { name: training-orchestrator, namespace: aegis-system }
spec:
  podSelector: { matchLabels: { app.kubernetes.io/name: training-orchestrator } }
  policyTypes: [Ingress, Egress]
  ingress:
    - from:
        - podSelector: { matchLabels: { app.kubernetes.io/name: api-gateway } }
      ports: [{ port: 8270, protocol: TCP }]
    - from:
        - namespaceSelector: { matchLabels: { kubernetes.io/metadata.name: aegis-observability } }
          podSelector: { matchLabels: { app.kubernetes.io/name: prometheus } }
      ports: [{ port: 8271, protocol: TCP }]
