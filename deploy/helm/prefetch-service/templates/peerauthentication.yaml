apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata: { name: prefetch-service-mtls-strict, namespace: aegis-system }
spec:
  selector: { matchLabels: { app.kubernetes.io/name: prefetch-service } }
  mtls: { mode: STRICT }
