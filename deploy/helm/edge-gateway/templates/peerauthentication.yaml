apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata: { name: edge-gateway-mtls-strict, namespace: aegis-system }
spec:
  selector: { matchLabels: { app.kubernetes.io/name: edge-gateway } }
  mtls: { mode: STRICT }
