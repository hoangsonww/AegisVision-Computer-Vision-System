apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata: { name: rule-engine-mtls-strict, namespace: aegis-system }
spec:
  selector: { matchLabels: { app.kubernetes.io/name: rule-engine } }
  mtls: { mode: STRICT }
