apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata: { name: rule-engine, namespace: aegis-system }
spec:
  podSelector: { matchLabels: { app.kubernetes.io/name: rule-engine } }
  policyTypes: [Ingress, Egress]
  ingress:
    - from:
        - podSelector: { matchLabels: { app.kubernetes.io/name: api-gateway } }
      ports: [{ port: 8210, protocol: TCP }]
    - from:
        - namespaceSelector: { matchLabels: { kubernetes.io/metadata.name: aegis-observability } }
          podSelector: { matchLabels: { app.kubernetes.io/name: prometheus } }
      ports: [{ port: 8211, protocol: TCP }]
