apiVersion: v1
kind: Service
metadata: { name: tenant-service, namespace: aegis-system, labels: { app.kubernetes.io/name: tenant-service } }
spec:
  selector: { app.kubernetes.io/name: tenant-service }
  ports:
    - { name: http, port: 8300, targetPort: 8300, appProtocol: http }
    - { name: metrics, port: 8301, targetPort: 8301 }
