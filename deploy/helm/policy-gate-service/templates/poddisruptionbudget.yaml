apiVersion: policy/v1
kind: PodDisruptionBudget
metadata: { name: policy-gate-service, namespace: aegis-system }
spec:
  minAvailable: 50%
  selector: { matchLabels: { app.kubernetes.io/name: policy-gate-service } }
