{{ '{{- if .Values.serviceMonitor.enabled }}' }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata: { name: policy-gate-service, namespace: aegis-system, labels: { release: prometheus } }
spec:
  selector: { matchLabels: { app.kubernetes.io/name: policy-gate-service } }
  endpoints: [{ port: metrics, interval: {{ '{{ .Values.serviceMonitor.interval }}' }}, path: /metrics }]
{{ '{{- end }}' }}
