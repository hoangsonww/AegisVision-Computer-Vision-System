apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata: { name: policy-gate-service, namespace: aegis-system }
spec:
  podSelector: { matchLabels: { app.kubernetes.io/name: policy-gate-service } }
  policyTypes: [Ingress, Egress]
  ingress:
    - from:
        - podSelector: { matchLabels: { app.kubernetes.io/name: agent-service } }
        - podSelector: { matchLabels: { app.kubernetes.io/name: api-gateway } }
      ports: [{ port: 8410, protocol: TCP }]
    - from:
        - namespaceSelector: { matchLabels: { kubernetes.io/metadata.name: aegis-observability } }
          podSelector: { matchLabels: { app.kubernetes.io/name: prometheus } }
      ports: [{ port: 8411, protocol: TCP }]
