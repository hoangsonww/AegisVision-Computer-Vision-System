apiVersion: v1
kind: Service
metadata: { name: policy-gate-service, namespace: aegis-system, labels: { app.kubernetes.io/name: policy-gate-service } }
spec:
  selector: { app.kubernetes.io/name: policy-gate-service }
  ports:
    - { name: http, port: 8410, targetPort: 8410, appProtocol: http }
    - { name: metrics, port: 8411, targetPort: 8411 }
