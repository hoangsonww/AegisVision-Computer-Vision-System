apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata: { name: model-registry-mtls-strict, namespace: aegis-system }
spec:
  selector: { matchLabels: { app.kubernetes.io/name: model-registry } }
  mtls: { mode: STRICT }
