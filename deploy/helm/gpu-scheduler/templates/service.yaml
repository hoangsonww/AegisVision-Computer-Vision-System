apiVersion: v1
kind: Service
metadata: { name: gpu-scheduler, namespace: aegis-system, labels: { app.kubernetes.io/name: gpu-scheduler } }
spec:
  selector: { app.kubernetes.io/name: gpu-scheduler }
  ports:
    - { name: http, port: 8200, targetPort: 8200, appProtocol: http }
    - { name: metrics, port: 8201, targetPort: 8201 }
