apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata: { name: gpu-scheduler, namespace: aegis-system }
spec:
  podSelector: { matchLabels: { app.kubernetes.io/name: gpu-scheduler } }
  policyTypes: [Ingress, Egress]
  ingress:
    - from:
        - podSelector: { matchLabels: { app.kubernetes.io/name: api-gateway } }
      ports: [{ port: 8200, protocol: TCP }]
    - from:
        - namespaceSelector: { matchLabels: { kubernetes.io/metadata.name: aegis-observability } }
          podSelector: { matchLabels: { app.kubernetes.io/name: prometheus } }
      ports: [{ port: 8201, protocol: TCP }]
