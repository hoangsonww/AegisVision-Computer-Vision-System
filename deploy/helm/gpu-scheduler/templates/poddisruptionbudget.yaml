apiVersion: policy/v1
kind: PodDisruptionBudget
metadata: { name: gpu-scheduler, namespace: aegis-system }
spec:
  minAvailable: 50%
  selector: { matchLabels: { app.kubernetes.io/name: gpu-scheduler } }
