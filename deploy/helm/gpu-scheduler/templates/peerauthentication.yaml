apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata: { name: gpu-scheduler-mtls-strict, namespace: aegis-system }
spec:
  selector: { matchLabels: { app.kubernetes.io/name: gpu-scheduler } }
  mtls: { mode: STRICT }
