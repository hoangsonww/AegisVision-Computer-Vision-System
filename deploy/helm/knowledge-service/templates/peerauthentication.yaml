apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata: { name: knowledge-service-mtls-strict, namespace: aegis-system }
spec:
  selector: { matchLabels: { app.kubernetes.io/name: knowledge-service } }
  mtls: { mode: STRICT }
