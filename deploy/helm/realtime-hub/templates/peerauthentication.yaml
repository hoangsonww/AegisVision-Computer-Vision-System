apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata: { name: realtime-hub-mtls-strict, namespace: aegis-system }
spec:
  selector: { matchLabels: { app.kubernetes.io/name: realtime-hub } }
  mtls: { mode: STRICT }
