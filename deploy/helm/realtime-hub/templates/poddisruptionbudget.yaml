apiVersion: policy/v1
kind: PodDisruptionBudget
metadata: { name: realtime-hub, namespace: aegis-system }
spec:
  minAvailable: 50%
  selector: { matchLabels: { app.kubernetes.io/name: realtime-hub } }
