apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata: { name: slo-watchdog-mtls-strict, namespace: aegis-system }
spec:
  selector: { matchLabels: { app.kubernetes.io/name: slo-watchdog } }
  mtls: { mode: STRICT }
