apiVersion: v1
kind: Service
metadata: { name: cost-accounting, namespace: aegis-system, labels: { app.kubernetes.io/name: cost-accounting } }
spec:
  selector: { app.kubernetes.io/name: cost-accounting }
  ports:
    - { name: http, port: 8330, targetPort: 8330, appProtocol: http }
    - { name: metrics, port: 8331, targetPort: 8331 }
