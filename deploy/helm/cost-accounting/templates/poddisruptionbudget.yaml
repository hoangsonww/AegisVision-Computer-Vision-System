apiVersion: policy/v1
kind: PodDisruptionBudget
metadata: { name: cost-accounting, namespace: aegis-system }
spec:
  minAvailable: 50%
  selector: { matchLabels: { app.kubernetes.io/name: cost-accounting } }
