apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata: { name: cost-accounting-mtls-strict, namespace: aegis-system }
spec:
  selector: { matchLabels: { app.kubernetes.io/name: cost-accounting } }
  mtls: { mode: STRICT }
