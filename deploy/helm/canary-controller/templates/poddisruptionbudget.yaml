apiVersion: policy/v1
kind: PodDisruptionBudget
metadata: { name: canary-controller, namespace: aegis-system }
spec:
  minAvailable: 50%
  selector: { matchLabels: { app.kubernetes.io/name: canary-controller } }
