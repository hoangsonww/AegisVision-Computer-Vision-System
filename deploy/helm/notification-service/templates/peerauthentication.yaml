apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata: { name: notification-service-mtls-strict, namespace: aegis-system }
spec:
  selector: { matchLabels: { app.kubernetes.io/name: notification-service } }
  mtls: { mode: STRICT }
