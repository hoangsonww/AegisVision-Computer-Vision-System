apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata: { name: metering-service-mtls-strict, namespace: aegis-system }
spec:
  selector: { matchLabels: { app.kubernetes.io/name: metering-service } }
  mtls: { mode: STRICT }
