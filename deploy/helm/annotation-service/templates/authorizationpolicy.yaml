apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata: { name: annotation-service, namespace: aegis-system }
spec:
  selector: { matchLabels: { app.kubernetes.io/name: annotation-service } }
  action: ALLOW
  rules:
    - from:
        - source: { principals: [cluster.local/ns/aegis-system/sa/api-gateway] }
      to: [{ operation: { ports: ["8260"] } }]
    - from:
        - source: { principals: [cluster.local/ns/aegis-observability/sa/prometheus] }
      to: [{ operation: { ports: ["8261"] } }]
