apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata: { name: annotation-service-mtls-strict, namespace: aegis-system }
spec:
  selector: { matchLabels: { app.kubernetes.io/name: annotation-service } }
  mtls: { mode: STRICT }
