apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata: { name: shadow-inference-service-mtls-strict, namespace: aegis-system }
spec:
  selector: { matchLabels: { app.kubernetes.io/name: shadow-inference-service } }
  mtls: { mode: STRICT }
