apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata: { name: shadow-inference-service, namespace: aegis-system }
spec:
  podSelector: { matchLabels: { app.kubernetes.io/name: shadow-inference-service } }
  policyTypes: [Ingress, Egress]
  ingress:
    - from:
        - podSelector: { matchLabels: { app.kubernetes.io/name: canary-controller } }
        - podSelector: { matchLabels: { app.kubernetes.io/name: api-gateway } }
      ports: [{ port: 8470, protocol: TCP }]
    - from:
        - namespaceSelector: { matchLabels: { kubernetes.io/metadata.name: aegis-observability } }
          podSelector: { matchLabels: { app.kubernetes.io/name: prometheus } }
      ports: [{ port: 8471, protocol: TCP }]
