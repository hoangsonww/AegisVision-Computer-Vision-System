apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata: { name: auth-proxy-mtls-strict, namespace: aegis-system }
spec:
  selector: { matchLabels: { app.kubernetes.io/name: auth-proxy } }
  mtls: { mode: STRICT }
