apiVersion: policy/v1
kind: PodDisruptionBudget
metadata: { name: stream-manager, namespace: aegis-system }
spec:
  minAvailable: 50%
  selector: { matchLabels: { app.kubernetes.io/name: stream-manager } }
