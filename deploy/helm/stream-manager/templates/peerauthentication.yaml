apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata:
  name: stream-manager-mtls-strict
  namespace: aegis-system
spec:
  selector: { matchLabels: { app.kubernetes.io/name: stream-manager } }
  mtls: { mode: STRICT }
