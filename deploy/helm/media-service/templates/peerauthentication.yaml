apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata: { name: media-service-mtls-strict, namespace: aegis-system }
spec:
  selector: { matchLabels: { app.kubernetes.io/name: media-service } }
  mtls: { mode: STRICT }
